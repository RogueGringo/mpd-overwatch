services:
  - type: web
    name: mpd-overwatch
    runtime: python
    buildCommand: pip install -e .
    startCommand: gunicorn mpd_overwatch.wsgi:server --bind 0.0.0.0:$PORT --workers 2 --timeout 120
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
